# See examples/relay/full.yaml for the complete set of nip11 fields and
# which ones `ncli relay` actually honors from config vs. always overwrites.
nip11:
  name: "ncli Auth Relay"
  pubkey: "d3345d36e7885ad6340263309a96e0821b22e176ebc721098492063df5516086"
  contact: "admin@example.com"
  # url: required when auth_required is true.
  url: "ws://localhost:5501"
  limitation:
    auth_required: true

logdir: "./data/logs"
store: "./data/db/auth.db"
port: 5501

cache:
  search:
    enabled: true
    host: "http://127.0.0.1:7700"
    key: "masterKey"
    index_name: "ncli_auth"
    batch_size: 1000
    max_ch_size: 10000
